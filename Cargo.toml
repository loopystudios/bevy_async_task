[package]
name = "bevy-async-task"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
futures = "0.3"
# WASM compatible features only!
tokio = { version = "1", default-features = false, features = [
  "sync",
  "macros",
  "io-util",
  "rt",
  "time",
] }
async-std = "1.12"
bevy = "0.10"

[dev-dependencies]
futures-timer = "3.0"
